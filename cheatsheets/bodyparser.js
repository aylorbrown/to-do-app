const bodyParser = require('body-parser');
const parseForm = bodyParser.urlencoded({
    extended: true
});

const parseForm = bodyparser


//usually stored in index.js, or wherever you are using routes
//npm i body-parser