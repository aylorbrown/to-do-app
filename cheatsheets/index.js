//npm init -y
//generate a schema and seed file first
//package.json edit your scripts, the stuff you add will allow you to generate a databse
//npm run db:create, npm run db:reset----this creates a database, the reset will populate the databse with the seed file
//set up your connection in a models folder, which exports a variable that contains the connection.
//npm install as you go depending on what you need
//start building routes, accessing the data that you need (.get, .post) ----normally this will be in index after we create the express app
//then start rendering , building out our html
//css then styling
